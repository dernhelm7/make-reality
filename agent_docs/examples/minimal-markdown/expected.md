# Expected Outcome

- The resolved work title is `First Room`.
- The home-page `Read` entry label is `First Room`, with `first-room` as its reference label.
- The home page shows the title, visible cover line, global links, and `Read` heading from `home.md`; the metadata summary and feed subtitle stay in `site.toml`.
- The home page puts the global links beneath the cover header, keeps the cover header on one home-column measure, and gives the `Read` index a wider measure.
- The home page advertises one Atom feed at `/feed.xml`, and that feed carries the browser-facing subscription guide from `feed.md` and one styled entry for `First Room`.
- The home page can reveal a feed preview from `feed.md` without moving the cover or the `Read` index. The preview keeps normal paragraph rhythm, ends with a `Follow` link to the feed, and pointer exit does not clear the shown preview.
- The work falls into the fallback section `Other works`.
- The first Markdown heading appears inside the work body beneath the work header, not as a second page-level title.
- The left rail shows `Other works` as a static section label and `First Room` as the current entry.
- The work page shows no current-work heading index.
