# Expected Outcome

- The home page reflects the title, cover text, links, contents heading, sections, and works authored in the source files.
- The feed reflects `site.toml`, `feed.md`, and the work metadata for the same build URL.
- The work page renders the derived work title, metadata, body, and current contents entry from the source files.
