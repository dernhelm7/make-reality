# Expected Outcome

- The home-page contents index preserves the section order from `home.md`.
- The home-page contents sections are flat siblings in one source-ordered column, with CSS responsible for the wide sticky metadata track.
- Works appear under the sections matched by their parent folder under `works/`.
- Empty authored sections remain visible on the home page.
- A work page rail links the resolved section label to its matching home-page contents section and shows the current work inside that section.
