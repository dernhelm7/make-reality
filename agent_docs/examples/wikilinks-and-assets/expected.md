# Expected Outcome

- The build succeeds.
- `[[Garden Path]]` resolves to the canonical URL for `garden-path`.
- `[[Missing Note|Elsewhere]]` becomes visible plain text `Elsewhere` with no hyperlink.
- `lantern.svg` is published because the work body references it.
- `unused.svg` is not published because the work body does not reference it.
